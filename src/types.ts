export type navLinks = {
  href: string;
  key: string;
  label: string;
};

export type projectItem = {
  id: string;
  title: string;
  imageSrc: string;
  text: string;
};
export type reviewItem = {
  id: string;
  name: string;
  text: string;
  imageSrc: string;
};
